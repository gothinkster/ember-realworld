<div class="article-page">
  <div class="banner">
    <div class="container">
      <h1>{{model.title}}</h1>
      {{article-author-meta article=model onFavoriteArticle=(action (route-action "favoriteArticle" model)) onFollowAuthor=(action (route-action "followAuthor" model.author))}}
    </div>
  </div>
  <div class="container page">
    <div class="row article-content">
      <div class="col-md-12">
        {{articleBody}}
        <ul class="tag-list"></ul>
      </div>
    </div>
    <hr>
    <div class="article-actions">
      {{article-author-meta article=model onFavoriteArticle=(action (route-action "favoriteArticle" model)) onFollowAuthor=(action (route-action "followAuthor" model.author))}}
    </div>
    <div class="row">
      <div class="col-xs-12 col-md-8 offset-md-2">
        {{#if session.isLoggedIn}}
          <form class="card comment-form" onsubmit={{action (route-action "createComment" model newComment)}}>
            <div class="card-block">
              {{textarea class="form-control" placeholder="Write a comment..." rows="3" value=newComment}}
            </div>
            <div class="card-footer">
              <img class="comment-author-img" alt="">
              <button class="btn btn-sm btn-primary" type="submit">
                Post Comment
              </button>
            </div>
          </form>
        {{/if}}
        {{#each comments key="id" as | comment |}}
          <div class="card">
            <div class="card-block">
              <p class="card-text">{{comment.body}}</p>
            </div>
            <div class="card-footer">
              {{#link-to "profile" comment.author.id class="comment-author"}}
                <img src={{comment.author.image}} class="comment-author-img" alt="comment author image">
                &nbsp;
                {{comment.author.username}}
              {{/link-to}}
              <span class="date-posted">{{comment.createdAt}}</span>
              {{#if (eq session.user.username comment.author.username)}}
                <span class="mod-options">
                  {{!-- API doesn't seem to support editing comments.
                  <i class="ion-edit"></i>
                  --}}
                  <button class="ion-trash-a" type="button" {{action (route-action "deleteComment" comment)}}></button>
                </span>
              {{/if}}
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>